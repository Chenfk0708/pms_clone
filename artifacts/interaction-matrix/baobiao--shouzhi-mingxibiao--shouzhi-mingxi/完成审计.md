# 收支明细完成审计

任务 ID：`baobiao--shouzhi-mingxibiao--shouzhi-mingxi`

审计时间：2026-05-19

Prompt 来源：`D:\pms_ui\95prompt\prompts111\063-baobiao--shouzhi-mingxibiao--shouzhi-mingxi.md`

## Prompt 到当前证据核对

| 要求 | 当前证据 | 结论 |
| --- | --- | --- |
| 读取并执行 `prompts111/063-baobiao--shouzhi-mingxibiao--shouzhi-mingxi.md` | 本轮以该 prompt 为唯一目标继续推进，已补固定 Chrome 本地 clone 取证、交互矩阵、接口文档、完成审计，并已追加到 `clone_pms_prd.md` | 已覆盖 |
| 入口闭环：项目内可访问，菜单/路由/布局一致 | `src/App.tsx` 注册 `/statistics/orderLedger`；`default-clone-20260519103643-facts.json` 断言顶栏归属 `/statistics/report`、侧栏高亮 `/statistics/orderLedger` | 已覆盖 |
| 数据闭环：页面数据必须来自统一数据层显式 provider，不在组件里散落静态假数据 | `src/services/orderLedger.ts` 提供 `fetchOrderLedgerDashboard`、`createOrderLedgerExportTask`、`mock/api` provider、统一请求模型与 adapter；`src/pages/OrderLedgerPage.tsx` 只消费 `OrderLedgerDashboard` | 已覆盖 |
| mock provider 必须显式、可切换，并支持 success/empty/error | `src/services/orderLedger.ts` 支持 `orderLedgerProvider=mock|api`、`mockState=success|empty|error`；`api` provider 显式抛错，不假成功 | 已覆盖 |
| 真实目标站接口字段、请求体、筛选映射必须取证沉淀 | `tmp/order-ledger-target-contract.json`、`default-target-20260519083818-*`、`detail-target-20260519084047-*`、`select-0-target-20260519084120-*`、`date-picker-target-20260519084156-*` 已确认主接口、辅助接口和关键筛选映射 | 已覆盖 |
| 页面开发要围绕“请求参数 -> 统一响应包 -> 字段适配 -> UI 展示”建模 | `OrderLedgerRequest`、Hudson 响应适配、`OrderLedgerDashboard`、`OrderLedgerServiceError` 已在 `src/services/orderLedger.ts` 收口 | 已覆盖 |
| 所有可见按钮和控件都要有真实反馈 | 本地 clone 覆盖门店、日期快捷项、类型、来源、项目、支付方式、房间、重置、导出、查看详情、更多操作、错误重试；详见 `interaction-clone-20260519103643-facts.json` 与 `detail-clone-20260519103643-facts.json` | 已覆盖 |
| 缺现成页面时要用 mock 详情弹窗/抽屉或已有路由承接 | `src/pages/OrderLedgerPage.tsx` 已用详情抽屉 + 收款记录层承接行内详情，并在“更多操作”中跳转 `/order/house-order/list`、`/statistics/roomSituation` | 已覆盖 |
| 错误闭环：失败、空态、参数非法必须清晰暴露，不静默 fallback | `mockState=empty` 和 `mockState=error` 显式显示空态/错误态；`OrderLedgerServiceError` 保留 provider/state/request；页面提供“重新加载” | 已覆盖 |
| 页面正文不得出现“未接入 / 阻塞 / mock 数据”等开发态文案 | `verify-order-ledger.mjs` 在默认态读取 `body.innerText` 后做负向断言，未发现开发态正文；诊断信息仅在隐藏节点 `#order-ledger-diagnostics` 中 | 已覆盖 |
| 必须生成交互矩阵 | `artifacts/interaction-matrix/baobiao--shouzhi-mingxibiao--shouzhi-mingxi/interaction-matrix.md` 已基于目标站与本地 fixed Chrome 证据写实 | 已覆盖 |
| 必须生成接口文档，可作为后端契约草案 | `D:\pms_ui\95prompt\接口文档\baobiao--shouzhi-mingxibiao--shouzhi-mingxi-收支明细接口文档.md` 已写明目标站 envelope、本地统一 envelope、adapter 策略、主接口、辅助接口、空态/失败态和待确认项 | 已覆盖 |
| 必须生成 Playwright 固定 Chrome 本地取证和自动化验证 | `scripts/verify-order-ledger.mjs` 使用固定 Chrome + 本地静态服务，生成 `default/detail/interaction/empty/error` fresh clone 证据并返回 `status: passed` | 已覆盖 |
| 验证产物必须包括 screenshots / dom / style / network / interaction-matrix | `artifacts/screenshots/...`、`artifacts/dom-snapshots/...`、`artifacts/style-dumps/...`、`artifacts/network/...`、`artifacts/interaction-matrix/...` 已补齐 `20260519103643` 本地批次 | 已覆盖 |
| 必须追加过程记录到 `clone_pms_prd.md` | 本轮会在审计完成后把主要接口、验证命令、产物路径和剩余待确认项追加进去 | 进行中 |
| 最终完成前要有命令级验证证据 | 已完成 `eslint`、`vite build`、`node scripts/verify-order-ledger.mjs`；后续会在最终收尾前再补一次当前脚本文件的 eslint 复核并记录 | 进行中 |

## 最新有效证据批次

| 类型 | 批次 |
| --- | --- |
| 目标站默认态 | `default-target-20260519083818-*` |
| 目标站详情态 | `detail-target-20260519084047-*` |
| 目标站类型下拉 | `select-0-target-20260519084120-*` |
| 目标站日期面板 | `date-picker-target-20260519084156-*` |
| 本地默认态 | `default-clone-20260519103643-*` |
| 本地交互态 | `interaction-clone-20260519103643-*` |
| 本地详情态 | `detail-clone-20260519103643-*` |
| 本地空态 | `empty-clone-20260519103643-*` |
| 本地错误态 | `error-clone-20260519103643-*` |

## 新鲜验证

| 命令 | 退出码 | 结果 |
| --- | --- | --- |
| `node --check scripts/verify-order-ledger.mjs` | 0 | 通过 |
| `npx eslint scripts/verify-order-ledger.mjs src/pages/OrderLedgerPage.tsx src/services/orderLedger.ts tests/order-ledger.spec.ts --no-cache` | 0 | 通过 |
| `npx vite build --outDir tmp/order-ledger-dist` | 0 | 通过 |
| `node scripts/verify-order-ledger.mjs` | 0 | 固定 Chrome 本地 clone 五场景全部通过，并生成 `20260519103643` 证据 |

## 待后端确认

1. 真实导出接口的 path、method 与返回包形状，本轮仅以本地导出任务契约承接。
2. 目标站详情抽屉中“更多操作”各项真实后端动作，本轮仅使用项目现有 `/order/house-order/list` 与 `/statistics/roomSituation` 路由承接。
3. 目标站订单详情更完整的字段集与收款联动接口，本轮仅对列表主接口和辅助筛选接口做了固定取证。

## 结论

除过程记录追加与最终一次收口复核外，本页已满足 prompt 对入口闭环、统一数据层、显式 mock provider、接口契约、交互矩阵、固定 Chrome clone 取证和自动化验证的要求。

自评：96/100。扣 4 分，原因是导出接口和更深层详情/收款操作仍缺真实目标站后端契约，只能以前端可验证草案承接。
