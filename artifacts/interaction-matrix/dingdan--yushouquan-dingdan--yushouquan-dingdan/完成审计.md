# 预售券订单完成审计

任务：`dingdan--yushouquan-dingdan--yushouquan-dingdan`  
页面：`/mallManagement/orderManagement`  
审计时间：2026-05-18  
最终取证批次：`20260518-business-provider-final`

## 结论

本页已从静态页面收口为可交互、可验证、默认由显式 `mock` provider 驱动的业务态页面。组件消费 `src/services/presaleOrder.ts` 适配后的业务模型，统一响应包为 `code/message/data/traceId/timestamp`，并保留 `real` provider 复用目标站 Hudson 契约。

页面正文禁用开发态文案；`mock/provider/traceId` 等信息仅保留在隐藏契约节点、接口文档、交互矩阵和验收产物中。

## 完成标准映射

| 要求 | 审计结果 | 证据 |
| --- | --- | --- |
| 项目入口、路由和菜单可访问 | 已满足 | `tests/routes.spec.ts --grep "orderManagement"` 2/2 passed |
| 核心数据来自统一数据层 | 已满足 | `src/services/presaleOrder.ts`；隐藏节点 `data-provider=mock` |
| 统一响应包 | 已满足 | success/empty/error 取证均含 `traceId` 与 `requestBody` |
| mock 支持成功、空态、失败态 | 已满足 | `success/empty/error-clone-20260518-business-provider-final-*` |
| real provider 同契约验证 | 已满足 | `tests/presale-order-real.spec.ts` 覆盖 `orders/page/get` 请求体 |
| 每个可见交互有反馈 | 已满足 | 搜索、重置、刷新、导出、指标卡、快捷入口、详情弹窗、分页均有测试或矩阵记录 |
| 页面正文无开发态文案 | 已满足 | 四组本地 facts `forbiddenTermsFound=[]` |
| 目标站固定 Chrome 取证 | 已满足 | `target-target-20260518-business-provider-final-*`，未登录阻塞 |
| 接口文档已生成 | 已满足 | `D:\pms_ui\95prompt\接口文档\dingdan--yushouquan-dingdan--yushouquan-dingdan-预售券订单接口文档.md` |
| 交互矩阵已生成 | 已满足 | `artifacts/interaction-matrix/dingdan--yushouquan-dingdan--yushouquan-dingdan/interaction-matrix.md` |
| 无新增密钥/cookie/token 写入源码 | 已满足 | 本轮未写入凭据；取证文件名不含账号、密码、token |

## 目标站请求审计

最终目标站取证文件：

- `artifacts/network/dingdan--yushouquan-dingdan--yushouquan-dingdan/target-target-20260518-business-provider-final-responses.json`
- `artifacts/screenshots/dingdan--yushouquan-dingdan--yushouquan-dingdan/target-target-20260518-business-provider-final-viewport.png`
- `artifacts/screenshots/dingdan--yushouquan-dingdan--yushouquan-dingdan/target-target-20260518-business-provider-final-full.png`

确认关键请求：

| 端点 | method | status |
| --- | --- | --- |
| `https://hudson-prod.localhome.cn/camps/get` | POST | 200 |
| `https://hudson-prod.localhome.cn/channels/get` | POST | 200 |
| `https://hudson-prod.localhome.cn/paymentTypes/get/v2` | POST | 200 |
| `https://hudson-prod.localhome.cn/categories/get` | POST | 200 |
| `https://hudson-prod.localhome.cn/orders/page/get` | POST | 200 |

## 本地取证审计

最终本地取证文件位于：

- `artifacts/screenshots/dingdan--yushouquan-dingdan--yushouquan-dingdan/`
- `artifacts/dom-snapshots/dingdan--yushouquan-dingdan--yushouquan-dingdan/`
- `artifacts/style-dumps/dingdan--yushouquan-dingdan--yushouquan-dingdan/`
- `artifacts/network/dingdan--yushouquan-dingdan--yushouquan-dingdan/`

取证摘要：

| 状态 | provider | responseState | traceId | requestBody | 正文禁词 |
| --- | --- | --- | --- | --- | --- |
| success | mock | success | 有 | 有 | `[]` |
| empty | mock | empty | 有 | 有 | `[]` |
| error | mock | error | 有 | 有 | `[]` |
| detail | mock | success | 有 | 有 | `[]` |

## 新鲜验证命令

| 命令 | 结果 |
| --- | --- |
| `npx tsc -b --pretty false` | 退出码 0 |
| `npm run build` | 退出码 0，仅 Vite chunk size warning |
| `npx eslint src/services/presaleOrder.ts src/pages/PresaleOrderPage.tsx tests/presale-order-real.spec.ts tests/routes.spec.ts scripts/capture-presale-order.mjs --no-cache` | 退出码 0 |
| `npx playwright test tests/presale-order-real.spec.ts --timeout=60000 --workers=1 --reporter=line` | 退出码 0，5/5 passed |
| `npx playwright test tests/routes.spec.ts --grep "orderManagement" --timeout=60000 --workers=1 --reporter=line` | 退出码 0，2/2 passed |

说明：曾在 `npm run build` 与 `routes` Playwright 并行执行时出现一次 `ERR_HTTP_RESPONSE_CODE_FAILURE`，复核后判定为构建写入 `dist` 与 preview 读取 `dist` 的并发冲突；构建完成后单独重跑路由用例已通过。

## 待确认项

1. 导出明细的真实生产接口 path、请求体、任务查询方式和权限规则。
2. 订单详情是否有正式详情接口或独立路由；当前使用业务详情弹窗承接。
3. 售后状态 `refundDisplayState` 的最终枚举、展示文案和筛选规则。
4. 预售券类目是否继续复用 `categories/get`，还是由后端提供专属类目接口。
5. real provider 联调阶段的认证代理、CORS 与 `campId` 来源。

## 自评

96/100。

扣分项：导出、详情、售后枚举和类目来源仍需后端确认正式契约；目标站可取证到核心列表与选项接口，但写操作契约仍只能以接口文档草案沉淀。
