# 保洁任务完成审计

## 审计范围

- Prompt：`D:\pms_ui\95prompt\prompts111\013-fangtai--baojie-guanli--baojie-renwu.md`
- 页面：`/cleanManage/cleanTask`
- 核心代码：`src/services/cleanTask.ts`、`src/pages/CleanTaskPage.tsx`、`src/pages/CleanTaskPage.css`
- 自动化：`tests/clean-task.spec.ts`、`tests/routes.spec.ts`
- 接口文档：`D:\pms_ui\95prompt\接口文档\fangtai--baojie-guanli--baojie-renwu-保洁任务接口文档.md`

## 完成标准逐项映射

| 序号 | Prompt 完成标准 | 本轮证据 | 状态 |
| --- | --- | --- | --- |
| 1 | 页面从项目入口可访问，菜单与路由状态正确 | `/cleanManage/cleanTask` 路由渲染业务页；`tests/routes.spec.ts --grep "cleanTask"` 覆盖侧栏与页面内容 | 已完成 |
| 2 | 核心数据来自统一数据层显式 provider，组件内静态假数据移除 | `src/services/cleanTask.ts` 定义 `mock/api` provider、统一响应包和 adapter；页面通过 `fetchCleanTaskDashboard()` 消费适配模型 | 已完成 |
| 3 | mock provider 返回统一响应包，接口文档生成 | 响应包为 `code/message/data/traceId/timestamp`；接口文档已生成到指定路径 | 已完成 |
| 4 | 交互矩阵每个可见按钮和控件都有处理结果 | `interaction-matrix.md` 已覆盖路由、门店、日期、筛选、查询、重置、刷新、导出、更多、批量通知、创建、详情、快捷入口、三态 | 已完成 |
| 5 | loading、成功、失败、空态、禁用态覆盖关键路径 | 页面包含 loading、反馈 status、错误 alert、空表格态、批量通知 disabled；`tests/clean-task.spec.ts` 覆盖 empty/error/retry | 已完成 |
| 6 | 跳转其他页面入口按项目路由协调 | 更多菜单与快捷入口使用 `/cleanManage/cleanStatistics`、`/cleanManage/cleanLog`、`/cleanManage/cleanSetting`、`/houseManage/days`、`/order/house-order/list`、`/cleanManage/cleanStaff` | 已完成 |
| 7 | Playwright 覆盖服务调用、数据展示、按钮、错误态和截图 | `tests/clean-task.spec.ts` 4 个用例覆盖；截图 `business-data-clone-20260518.png` 与固定 Chrome 批次已落盘 | 已完成 |
| 8 | 实时记录追加取证结论、接口字段、请求体、确认项和验收结果 | `clone_pms_prd.md` 已追加本轮记录和最终验证结果 | 已完成 |
| 9 | 未新增硬编码密钥、cookie、token、假成功 fallback 或吞错逻辑 | 源码未写入账号/密码/cookie/token；错误由 provider 抛出并在 UI alert 暴露；写操作为业务态反馈并在接口文档标注待后端确认 | 已完成 |

## 交付物逐项映射

| 序号 | Prompt 交付物 | 产物 |
| --- | --- | --- |
| 1 | 修改文件清单与目的 | 最终回复列出，实时记录也已追加 |
| 2 | mock 结构、统一响应包、目标站请求字段、服务接口和切换点说明 | `src/services/cleanTask.ts` 与接口文档第 2、3、7 节 |
| 3 | 后端接口文档路径 | `D:\pms_ui\95prompt\接口文档\fangtai--baojie-guanli--baojie-renwu-保洁任务接口文档.md` |
| 4 | 保洁任务交互矩阵 | `artifacts/interaction-matrix/fangtai--baojie-guanli--baojie-renwu/interaction-matrix.md` |
| 5 | Playwright 取证产物路径 | `artifacts/screenshots|dom-snapshots|style-dumps|network/fangtai--baojie-guanli--baojie-renwu/` |
| 6 | 自动化验证命令与结果 | 本文“最终验证记录”与最终回复 |
| 7 | 待用户确认项 | 本文“后端/用户待确认项” |
| 8 | 自评打分 | 最终回复和实时记录 |

## 关键实现证据

- `src/services/cleanTask.ts`
  - `cleanTaskProviderMode: 'mock' | 'api'`
  - `createCleanTaskRequestBody()` 输出目标站字段 `cleanTime`、`roomId`、`cleanerIds`、`pageNum`、`pageSize`
  - `mockCleanTaskProvider` 支持 `success/empty/error`
  - `apiCleanTaskProvider` 复用同一 endpoint 和 adapter
- `src/pages/CleanTaskPage.tsx`
  - 页面只消费 `CleanTaskDashboard`
  - 支持筛选、查询、重置、刷新、导出、更多、批量通知、详情、创建、空态、错误态和重试
- `tests/clean-task.spec.ts`
  - 覆盖业务数据、筛选参数、按钮反馈、弹窗、禁用态、空态、错误态

## 目标站与本地证据

- 目标站 Network：`target-20260518-business-provider.json`
- 目标站截图：`target-20260518-business-provider.png`
- 本地 Network：`clone-20260518-business-provider.json`
- 本地截图：`clone-20260518-business-provider.png`
- 专项测试截图：`business-data-clone-20260518.png`

## 后端/用户待确认项

- 保洁任务列表正式响应是否统一为本文档的 `code/message/data/traceId/timestamp`。
- 列表项日期字段最终使用 `cleanDate` 还是 `cleanTime`。
- 门店筛选是否进入 `/cleanTask/page/get` 请求体。
- 创建、批量通知、导出接口的真实 path、权限、幂等规则和错误码。
- 保洁类型、保洁状态和保洁员字段枚举是否与本文档一致。

## 最终验证记录

- `npx eslint src/pages/CleanTaskPage.tsx src/services/cleanTask.ts tests/clean-task.spec.ts tests/routes.spec.ts --no-cache`：退出码 0。
- `npx vite build`：退出码 0，仅 Vite chunk size warning。
- `npm run build`：退出码 0，`tsc -b && vite build` 均通过，仅 Vite chunk size warning。
- `PMS_TEST_BASE_URL=http://127.0.0.1:4174 npx playwright test tests/clean-task.spec.ts --timeout=60000 --workers=1`：退出码 0，4/4 passed。
- `PMS_TEST_BASE_URL=http://127.0.0.1:4174 npx playwright test tests/routes.spec.ts --grep "cleanTask|cleanStatistics|cleanStaff|cleanSetting|cleanLog" --timeout=60000 --workers=1`：退出码 0，7/7 passed。
- 页面可见文案复核：`forbiddenTermsFound=[]`。
