# 电子房价牌交互矩阵

记录时间：2026-05-16 13:47:34 +08:00

| 区域 | 元素/按钮 | 目标站行为 | 触发请求 | 本地现状 | 改善动作 | 验收方式 |
| --- | --- | --- | --- | --- | --- | --- |
| 页面入口 | 房态 > 房价管理 > 电子房价牌 | 进入 `/houseManage/priceBoard`，左侧房价管理展开，电子房价牌高亮，主内容展示订阅商品卡和商品详情 | 首屏会触发 `POST /edition/resource/get`、`POST /paymentTypes/get/v2`、`POST /weiRoomCategories/page/get` 及壳层请求 | 路由和菜单已存在；商品标题、描述、价格、购买信息仍写死在组件内 | 接入本页服务读取订阅商品、支付方式和房价牌房型接口；请求失败暴露阻塞 | Playwright 路由测试断言页面状态和请求 URL；target/local 网络清单落盘 |
| 顶部商品卡 | 去开通 | 打开购买详情视图，展示商品详情图、购买信息、时长选项、协议勾选和立即购买 | 复用已加载商品资源；购买页会继续请求 `paymentTypes/get/v2` 等支付配置 | 可打开详情，但没有真实请求状态，也没有接口失败反馈 | 详情页显示真实请求连接状态；接口失败时保留阻塞提示和重试入口，不假成功 | Playwright 点击后断言详情视图、连接状态、请求记录 |
| 商品详情 | 宣传图/购买详情图 | 展示目标站 OSS 资源图片 | 图片资源来自目标站 OSS，旧实现已下载到本地静态资源 | 图片可见，属于静态资源复刻，不等于业务数据闭环 | 保留真实静态资源，业务文案和价格改由接口适配结果驱动 | 图片 natural size 断言 + 网络状态断言 |
| 购买信息 | 1年/2年单选 | 切换购买时长，金额随时长变化；选中态变化 | 不额外请求，使用订阅资源返回的价格阶梯 | 旧实现 radio 使用 `defaultChecked`，价格固定 `¥150.9`，切换无业务反馈 | 将 duration 放入状态，按接口价格显示金额和选中态 | Playwright 切换 2 年后断言金额/选中态变化 |
| 购买信息 | 协议勾选/取消 | 未勾选时点击立即购买显示协议校验提示 | 不请求 | 已有校验提示 | 保留，并禁用重复提交；提示清晰暴露 | Playwright 取消勾选后断言校验提示 |
| 购买信息 | 立即购买 | 目标站打开微信支付弹层，展示支付二维码、支付方式、倒计时 | 支付相关接口包括 `paymentTypes/get/v2`，二维码/下单能力当前未在本地可安全调用 | 旧实现直接打开静态二维码弹层，属于假成功风险 | 仅在真实商品配置加载成功且协议同意后打开支付弹层；支付下单接口未接入时明确显示“真实支付下单未接入”阻塞，不伪造成功 | Playwright 点击后断言弹层和阻塞说明，不断言假支付成功 |
| 支付弹层 | 关闭 | 关闭弹层，返回购买信息 | 不请求 | 已可关闭 | 保留关闭反馈 | Playwright 点击关闭后断言弹层消失 |
| 错误态 | 真实接口失败/登录失效/CORS | 目标站应暴露失败、无权限或登录态问题 | 失败请求状态码和错误信息 | 旧实现无错误态，因为不请求业务接口 | 新增错误 banner、重试按钮和请求摘要，不吞错 | Playwright route abort 模拟失败后断言错误和重试入口 |
| 空态/阻塞 | `weiRoomCategories/page/get` 无数据或不可达 | 目标站仍保留订阅商品展示；具体电子房价牌房型数据可能为空或受权限限制 | `POST /weiRoomCategories/page/get` | 旧实现未展示数据接口状态 | 展示“房价牌房型接口返回数量/阻塞原因”，不脑补列表 | Playwright 成功/空返回测试断言结构不崩溃 |
