# 其他价格交互矩阵

任务：`fangtai--fangjia-guanli--qita-jiage`

取证来源：固定 Chrome + `playwright/.auth/pms-user.json` 访问 `https://minsubao.localhome.cn/houseManage/otherPrice`，2026-05-16 13:47 新鲜取证 `isLoginBlocked=false`。

| 区域 | 元素/按钮 | 目标站行为 | 触发请求 | 本地现状 | 改善动作 | 验收方式 |
| --- | --- | --- | --- | --- | --- | --- |
| 项目入口 | 房态 > 房价管理 > 其他价格 | URL 为 `/houseManage/otherPrice`，房态/房价管理/其他价格高亮，标题与布局进入其他价格页 | 首屏加载 `/camps/get`、`/roomCategories/page/get`、`/select/poi/page/get`、`/roomCategoryRules/get` 等 | 路由和菜单已接入 `PricePage`，数据仍主要来自组件静态数组 | 保留现有路由，补真实请求接入状态和错误暴露 | Playwright 路由测试断言 URL、侧栏高亮、页面主体和数据来源状态 |
| 顶部页签 | 杂费设置 / 活动设置 | 切换同页表格；活动设置显示 `+新增设置`、连住折扣和甩卖阶段列 | 页签切换不一定新增请求，数据来自首屏配置请求 | 可切换，但仅过滤硬编码数据 | 页签切换保留本地状态，显示已接入请求/阻塞状态 | 点击页签后断言表格、状态提示和无空响应 |
| 顶部筛选 | 渠道下拉 | 展开渠道选项，选择后表格按渠道过滤 | 目标站首屏请求含 `/select/calChannel4RoomCategory/get`、`/channels/get`；选择为本地筛选 | 可展开和筛选，但选项硬编码 | 渠道选项由真实请求适配，选择后触发重新加载或真实阻塞提示 | Playwright route 捕获请求 body，选择 `携程` 后断言 body.channelName/过滤状态 |
| 顶部筛选 | 房型下拉 | 展开房型多选/选择，表格按房型过滤 | `/roomCategories/page/get`、`/weiRoomCategories/page/get` | 可展开和筛选，但房型来自静态行 | 房型选项由真实请求适配，选择后请求参数与 UI 状态一致 | Playwright 选择房型后断言请求 body.keyword/roomCategoryIds 或阻塞提示 |
| 杂费表格 | 押金/可加客人数/加人费/餐食数量/佣金率 设置 | 点击 `设置` 打开改价抽屉，输入金额后提交；真实保存接口当前未完成取证 | 目标站打开抽屉时可见输入控件；保存接口未稳定取证，记录为未接入阻塞 | 抽屉可打开和关闭，保存直接关闭，属于假成功风险 | 保存不假成功，显示“保存接口未接入”阻塞；保留取消/关闭真实反馈 | 点击设置、输入、保存后断言 status 出现未接入提示且抽屉关闭/按钮恢复 |
| 活动表格 | +新增设置 | 打开活动设置抽屉，展示连住天数说明和添加按钮 | 目标站未观察到新增提交成功请求，记录为未接入阻塞 | 抽屉可打开，添加按钮无反馈 | 添加/保存显示未接入提示，不静默 | Playwright 点击新增、添加、保存后断言反馈 |
| 活动表格 | 甩卖阶段设置 | 点击 `设置` 打开改折扣抽屉，含第一阶段/第二阶段时间与折扣输入 | 保存接口未稳定取证，记录为未接入阻塞 | 抽屉可打开，保存直接关闭 | 表单校验空值，保存显示未接入提示，不假成功 | Playwright 点击设置、空保存、输入后保存断言校验/阻塞 |
| 状态反馈 | loading / error / empty / disabled | 接口加载中应明确，失败/无权限/登录失效应暴露，空数据不崩溃 | 请求失败或空响应 | 现有其他价格分支无请求态和错误态 | 增加 `role=status`、`role=alert`、空态和重试按钮 | 专项测试分别模拟成功、失败、空数据 |
| 跨页入口 | 房价管理页签导航 | 门市价、电子房价牌等跳转到已有路由 | 无额外接口 | 已通过 PriceTabs 导航 | 保留项目已有路径，不硬编码不存在路由 | 路由回归测试 |

当前阻塞：保存杂费、保存活动设置、改折扣提交接口尚未从目标站稳定取证到可复用契约，本轮必须暴露为“真实保存接口未接入”，不得显示假成功。
