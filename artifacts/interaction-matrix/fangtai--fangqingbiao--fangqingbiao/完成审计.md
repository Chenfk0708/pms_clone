# 房情表完成审计

## 目标

执行 `D:\pms_ui\95prompt\prompts111\012-fangtai--fangqingbiao--fangqingbiao.md`，在已有房情表页面基础上补齐：

1. 项目入口和菜单/路由状态闭环。
2. 默认显式 mock provider、统一响应包、字段 adapter、集中 real provider 切换。
3. 页面正文业务态展示，不出现开发态文案。
4. 空态、错误态、loading、按钮反馈和分页/切换交互。
5. 接口文档、交互矩阵、固定 Chrome artifacts 和自动化验证记录。
6. 实时记录文件追加进度、差异、验证和风险。

## Prompt 到证据清单

| 要求 | 证据 | 状态 |
| --- | --- | --- |
| 读取 prompts111 与原始 prompt | 本轮已读取 `D:\pms_ui\95prompt\prompts111\012-fangtai--fangqingbiao--fangqingbiao.md` 与原始 prompt；记录见 `clone_pms_prd.md` | 已完成 |
| 检查已有实现 | 已核对 `src/App.tsx`、`src/pages/RoomSituationPage.tsx`、`src/services/roomSituation.ts`、`tests/room-situation-interactions.spec.ts`、历史 artifacts | 已完成 |
| 从项目入口可访问 | `/statistics/roomSituation` 和 `/houseManage/houseStatus` 均由 `RoomSituationPage` 渲染；专项测试 5/5 覆盖 | 已完成 |
| 显式 mock provider | `src/services/roomSituation.ts` 默认 `resolveRoomSituationProvider()` 返回 `mock`，页面 DOM `data-provider=mock` | 已完成 |
| 统一响应包 | `RoomSituationApiResponse<T>` 使用 `code/message/data/traceId/timestamp`；门店、单日、远期 mock envelope 均使用该结构 | 已完成 |
| 字段 adapter | 组件只消费 `DailyRoomSituationRow` / `ForwardRoomSituationRow`；字段适配在 `adaptDailyRow`、`adaptForwardRow` | 已完成 |
| 集中切换 real provider | `roomSituationProvider=real`、`pms.roomSituation.provider`、`VITE_ROOM_SITUATION_PROVIDER`；测试断言 real 请求体 | 已完成 |
| 页面正文无开发态文案 | 专项测试断言默认页无 `mock provider/mock 数据/阻塞/未接入/后端未就绪/后端接口未完成` | 已完成 |
| 空态/错误态 | `roomSituationMockScenario=empty/error` 专项测试覆盖空态、错误提示和重试入口 | 已完成 |
| 交互矩阵 | `artifacts/interaction-matrix/fangtai--fangqingbiao--fangqingbiao/interaction-matrix.md` | 已完成 |
| 接口文档 | `D:\pms_ui\95prompt\接口文档\fangtai--fangqingbiao--fangqingbiao-房情表接口文档.md` | 已完成 |
| Playwright 取证产物 | 历史 target：`realdata-target-*20260516*`；本轮 local：`mock-default-clone.png`、`mock-error-clone.png` | 已完成 |
| 自动化验证 | `tests/room-situation-interactions.spec.ts` 5/5；`tests/routes.spec.ts --grep "roomSituation|houseStatus"` 1/1；`npm run lint -- --no-cache` 退出码 0 | 已完成 |
| 安全约束 | 对本任务源码、测试、矩阵和接口文档搜索账号、密码、token、Authorization、Bearer 无命中 | 已完成 |

## 验证命令

```text
npx playwright test tests/room-situation-interactions.spec.ts --timeout=60000 --workers=1 --reporter=line
结果：退出码 0，5/5 passed

npx playwright test tests/routes.spec.ts --grep "roomSituation|houseStatus" --timeout=60000 --workers=1 --reporter=line
结果：退出码 0，1/1 passed

npm run lint -- --no-cache
结果：退出码 0

npm run build
结果：最终复跑退出码 1，阻塞来自非房情表共享工作区红灯：缺 `CardVerificationPage.tsx`、`OrdersPage.tsx` 长租订单类型不一致
```

## 剩余风险

1. 全量 build 当前被非房情表页面阻断；房情表专项和全量 lint 已通过。
2. 目标站已取证响应为 `success/data`，接口文档建议后续统一为 `code/message/data/traceId/timestamp`；后端联调时需确认最终包格式。
3. 远期表日期列当前由前端根据开始日期和返回天数生成；是否由后端显式返回日期字段待确认。

## 自评

96/100。扣分项：全量 build 受共享工作区无关红灯影响；后端最终响应包和远期日期字段仍需联调确认。
