# 客户概况完成审计

任务：`scrm--kehu-gaikuang--kehu-gaikuang`

审计时间：2026-05-19

## 目标拆解

将 `/scrm/general` 客户概况从静态复刻升级为业务可用页面，满足：

1. 入口、路由、菜单高亮和页面布局协调。
2. 核心数据来自统一数据层的显式 mock provider，不在组件内散落静态假数据。
3. provider 使用统一响应包 `code/message/data/traceId/timestamp`，并能沉淀为后端接口契约。
4. 可见筛选、按钮、卡片、列表、弹窗、跳转都有反馈。
5. 覆盖 loading、success、empty、error、retry、disabled 等关键状态。
6. 使用固定 Chrome 对目标站和本地页取证，保留截图、DOM、style、network 和交互矩阵。
7. 生成接口文档并更新实时记录。
8. 自动化验证覆盖数据服务、交互、错误态、空态和截图回归。
9. 不把账号、密码、cookie、token、storage state 内容写入源码、测试、接口文档、记录或文件名；页面正文不展示开发态文案。

## Prompt-to-Artifact Checklist

| 要求 | 证据 | 状态 |
| --- | --- | --- |
| 读取新版 prompt 与原始 prompt | 本轮执行依据 `D:\pms_ui\95prompt\prompts111\033-scrm--kehu-gaikuang--kehu-gaikuang.md`；早前已确认原始 prompt `D:\pms_ui\95prompt\prompts\033-scrm--kehu-gaikuang--kehu-gaikuang.md` | 已完成 |
| 目标站固定 Chrome 取证 | `default-target-20260518-95-target.png`、`full-target-20260518-95-target.png`、`default-target-20260518-95-target.html`、`facts-target-20260518-95-target.json`、`responses-target-20260518-95-target.json` | 已完成 |
| 目标站交互事实 | `target-interactions-20260518-95.json`，记录授权、企微设置、推荐场景跳转 | 已完成 |
| 目标站网络字段沉淀 | network artifact 记录 `member/assetsInventory/get`、`member/growthTrend/get`、`wxCp/authInfo/get` 等；接口文档写入字段与待确认项 | 已完成 |
| 本地显式 provider | `src/services/scrmGeneral.ts` 定义 `SCRM_GENERAL_PROVIDER='mock'`、`SCRM_GENERAL_ENDPOINT`、请求体构造和 adapter | 已完成 |
| 统一响应包 | `ScrmGeneralEnvelope` 定义 `code/message/data/traceId/timestamp`；success/empty/error 均走 `createEnvelope()` | 已完成 |
| 组件只消费业务模型 | `src/pages/ScrmGeneralPage.tsx` 调用 `loadScrmGeneralData()`，渲染 `ScrmGeneralModel`；请求状态仅 hidden output 供测试读取 | 已完成 |
| 筛选闭环 | 开始日期、结束日期、门店、运营维度进入 `buildScrmGeneralRequestBody()`；Playwright 断言请求体 | 已完成 |
| 按钮反馈 | 查询、重置、刷新、导出、知道了、重试、指标详情、推荐场景、客户列表均在专项测试覆盖 | 已完成 |
| 跨页承接 | 客户列表 link 跳 `/customer/list`；企微授权跳 `/channels/private/setting/weComSetting`；推荐场景用弹窗和既有 route 承接 | 已完成 |
| 空态 | `scenario=empty` 返回空待办/来源，页面显示“当前条件暂无待办”“暂无来源数据” | 已完成 |
| 错误态 | `scenario=error` 抛 `ScrmGeneralServiceError`，页面显示 alert 和重试入口 | 已完成 |
| 本地取证 | 有效批次 `20260519-95-success-rerun`、`20260519-95-empty-rerun`、`20260519-95-error` 覆盖截图、DOM、style、network | 已完成 |
| 交互矩阵 | `artifacts/interaction-matrix/scrm--kehu-gaikuang--kehu-gaikuang/interaction-matrix.md` | 已完成 |
| 接口文档 | `D:\pms_ui\95prompt\接口文档\scrm--kehu-gaikuang--kehu-gaikuang-客户概况接口文档.md` | 已完成 |
| 实时记录 | `C:\Users\Administrator\Desktop\work\xxxxx\clone_pms_prd.md` 已追加客户概况记录 | 已完成 |
| 专项自动化 | `npx playwright test scrm-general.spec.ts --config=tmp/playwright-scrm-general.config.ts --timeout=60000 --workers=1 --reporter=line`，4/4 passed | 已完成 |
| 触达文件 lint | `npx eslint src/pages/ScrmGeneralPage.tsx src/services/scrmGeneral.ts tests/scrm-general.spec.ts --no-cache` 退出码 0；最新包含 `eslint.config.js` 的精确 lint 退出码 0 | 已完成 |
| 触达文件 TypeScript | `npx tsc --noEmit --ignoreConfig ... src/services/scrmGeneral.ts src/pages/ScrmGeneralPage.tsx tests/scrm-general.spec.ts` 退出码 0 | 已完成 |
| 项目 build | `npm run build` 退出码 0，仅 Vite chunk size warning | 已完成 |
| 项目 lint | `npm run lint -- --no-cache` 退出码 1，阻塞来自无关文件 `DistributionListPage.tsx`、`WechatServicePage.tsx` 的 hook 规则；客户概况触达文件精确 lint 通过 | 非本页阻塞 |
| 页面正文禁用文案 | Playwright bodyText 断言；对有效本地 DOM 搜索 `未接入/阻塞/后端未就绪/后端接口未完成/mock 数据/mock provider` 无命中 | 已完成 |
| 凭据安全 | 对客户概况源码、测试、交互矩阵、接口文档搜索账号、密码、Authorization、Bearer、hudson-access-token、appKey、accid 等无命中；新增文件名无凭据 | 已完成 |

## 发现与处理

- 首次本地 success/empty capture 批次 `20260519-95-success`、`20260519-95-empty` 捕获到空 DOM。根因是同一时间共享工作区无关 `DistributionDisplacementPage.css` 缺失导致 Vite 预转换失败；该问题由并行任务补齐后，已重采集 `20260519-95-success-rerun` 和 `20260519-95-empty-rerun`，重采集批次有效。
- `eslint .` 原本会扫描 `artifacts/test-results`，出现 ENOENT 或超时。已将生成目录 `artifacts`、`test-results`、`playwright-report` 加入 `eslint.config.js` 全局忽略，之后全量 lint 能真实执行并报告无关页面 hook 错误。

## 剩余风险

1. 后端仍需确认是否采用聚合接口 `/scrm/general/overview/get`，还是沿用目标站多个 Hudson 接口。
2. 粉丝总数、企微人数和客户来源排行的正式统计口径待后端确认。
3. 导出接口的正式 path、权限、幂等规则和异步任务状态待确认。
4. 全量 lint 当前有无关页面红灯，不影响客户概况触达文件，但需要对应页面任务处理。

## 结论

客户概况本页目标已闭环；未发现本页缺失、弱验证或未覆盖的 prompt 要求。自评：95/100，扣分来自正式后端接口与导出契约仍待联调确认，以及共享工作区全量 lint 存在无关页面红灯。
