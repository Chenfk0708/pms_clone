# 聊天工具栏完成审计

任务：`scrm--kehu-goutong--liaotian-gongjulan`  
页面：`/scrm/sidebarPreview`  
审计日期：`2026-05-19`

## 1. 目标重述

本任务的完成标准不是把旧订阅介绍页简单换皮，而是把聊天工具栏升级为：

1. 可从项目导航进入，菜单和路由状态正确。
2. 页面数据来自显式 `mock` provider，而不是组件内写死数据。
3. 服务层按统一响应包 `code/message/data/traceId/timestamp` 建模，并为后续后端联调保留 `api` provider 切换点。
4. 页面具备查询、重置、刷新、导出、详情、话术发送、快捷跳转、empty、error 等交互闭环。
5. 页面正文不展示开发态文案。
6. 已生成接口文档、交互矩阵、目标站取证和本地 success/empty/error/detail 取证。
7. 已完成可复现自动化验证。
8. 已把结论追加到 `C:\Users\Administrator\Desktop\work\xxxxx\clone_pms_prd.md`。

## 2. 提示词到产物核对清单

| 要求 | 证据 | 结论 |
| --- | --- | --- |
| 读取并执行 `prompts111/043-scrm--kehu-goutong--liaotian-gongjulan.md` | 实时记录已有启动记录；本轮继续按该任务收口 | 已满足 |
| 检查现有路由、页面、样式、测试、采集脚本 | `src/App.tsx`、`src/data/discovery.ts`、`src/pages/ScrmSidebarPreviewPage.tsx`、`src/pages/ScrmSidebarPreviewPage.css`、`tests/scrm-sidebar-preview.spec.ts`、`scripts/capture-scrm-sidebar-preview.mjs` | 已满足 |
| 导航入口闭环 | `src/App.tsx` 注册 `/scrm/sidebarPreview`；`src/data/discovery.ts` 侧边导航 `聊天工具栏` | 已满足 |
| 显式 mock provider | `src/services/scrmSidebarPreview.ts` 中 `scrmSidebarProviderMode: 'mock'` | 已满足 |
| 统一响应包 | 同文件 `ApiEnvelope<T>`、`createEnvelope()`、`assertEnvelope()` | 已满足 |
| 字段适配与组件隔离 | 同文件 `adaptScrmSidebarDashboard()`、`adaptConversation()`；页面只消费 `ScrmSidebarDashboard` | 已满足 |
| 查询/刷新/导出/详情/发送话术/快捷路由 | `src/pages/ScrmSidebarPreviewPage.tsx` 对应按钮与反馈逻辑 | 已满足 |
| empty/error 覆盖 | 页面 `mockState` 场景 + Playwright 第 3 条用例 | 已满足 |
| 页面正文禁止开发态文案 | `rg` 仅命中测试负向断言，源码和 DOM 快照未命中 | 已满足 |
| 接口文档 | `D:\\pms_ui\\95prompt\\接口文档\\scrm--kehu-goutong--liaotian-gongjulan-聊天工具栏接口文档.md` | 已满足 |
| 交互矩阵 | `artifacts/interaction-matrix/scrm--kehu-goutong--liaotian-gongjulan/interaction-matrix.md` | 已满足 |
| 目标站取证 | `artifacts/screenshots|dom-snapshots|style-dumps|network/.../*target-20260518-95-target*` | 已满足 |
| 本地取证 success/empty/error/detail | `artifacts/screenshots|dom-snapshots|style-dumps|network/.../*20260519-95-local-v2*` | 已满足 |
| 采集脚本支持新版业务态和多状态 clone | `scripts/capture-scrm-sidebar-preview.mjs` 的 `--state`、`gotoWithRetry()`、`waitForSurface()` | 已满足 |
| 自动化验证 | ESLint、Playwright 专项、`npm run build` 新鲜通过 | 已满足 |
| 追加实时记录 | `clone_pms_prd.md` 已追加本次补记条目 | 已满足 |

## 3. 新鲜验证证据

| 命令 | 结果 |
| --- | --- |
| `npx eslint src\\services\\scrmSidebarPreview.ts src\\pages\\ScrmSidebarPreviewPage.tsx tests\\scrm-sidebar-preview.spec.ts scripts\\capture-scrm-sidebar-preview.mjs --no-cache` | 退出码 `0` |
| `PMS_TEST_BASE_URL=http://127.0.0.1:43322 PMS_CHROME_PATH=C:/Users/Administrator/AppData/Local/Google/Chrome/Bin/chrome.exe npx playwright test tests/scrm-sidebar-preview.spec.ts --timeout=60000 --workers=1 --reporter=line` | 退出码 `0`，`3 passed` |
| `npm run build` | 退出码 `0`，`tsc -b && vite build` 成功 |
| `Invoke-WebRequest http://127.0.0.1:43322/scrm/sidebarPreview` | `HTTP 200` |

## 4. 弱证据与排除项

- Codex Browser 插件连接 `http://127.0.0.1:43322/scrm/sidebarPreview` 时在 BrowserStart 阶段超时，两次重试都失败。该插件未形成可用证据。
- 该风险不阻塞本任务完成判断，因为本地 preview 的 HTTP 200、固定 Chrome Playwright 专项 3/3 通过、以及本地多状态截图/DOM/network 取证已经直接覆盖页面运行态。
- 旧批次 `facts-clone-20260519-95-local.json` / `responses-clone-20260519-95-local.json` 使用了未带状态名的旧命名，不作为本次多状态命名规范的主要证据；`*-v2` 批次已补齐 success/empty/error/detail。

## 5. 待后端确认但不阻塞完成

1. 正式后端是采用聚合接口 `/scrm/sidebarPreview/dashboard`，还是拆为多接口。
2. 发送话术、导出任务、同步排序、详情接口的正式 path、错误码和幂等规则。
3. 渠道枚举、响应 SLA 口径、订单/房态跳转携参规则。

## 6. 审计结论

以当前工作树、产物目录和新鲜验证结果为准，本任务要求已闭环，剩余项仅为后端联调待确认，不影响“95 分业务可用页 + mock 契约 + 文档 + 取证 + 自动化验证”的完成判定。
