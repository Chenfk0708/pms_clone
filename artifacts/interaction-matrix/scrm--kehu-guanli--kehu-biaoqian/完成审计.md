# 客户标签完成审计

任务：`scrm--kehu-guanli--kehu-biaoqian`  
审计时间：2026-05-19

| 完成标准 | 结论 | 证据 |
| --- | --- | --- |
| `/customer/tag` 可访问，菜单与路由状态正确 | 已满足 | `src/App.tsx` 已有路由；`tests/customer-tag.spec.ts` 断言 SCRM 与客户标签菜单 `is-active` |
| 核心数据来自统一数据层显式 mock provider | 已满足 | `src/services/customerTag.ts` 定义 `customerTagProvider = 'mock'`、统一响应包和 adapter；页面只消费 `CustomerTagData` |
| mock provider 返回统一后端响应包 | 已满足 | `{ code, message, data, traceId, timestamp }`；接口文档给出成功、空态、失败 JSON |
| 接口文档已生成 | 已满足 | `D:\pms_ui\95prompt\接口文档\scrm--kehu-guanli--kehu-biaoqian-客户标签接口文档.md` |
| 交互矩阵覆盖所有可见控件 | 已满足 | `artifacts/interaction-matrix/scrm--kehu-guanli--kehu-biaoqian/interaction-matrix.md` |
| loading、成功、失败、空态、禁用态覆盖 | 已满足 | 专项 Playwright 覆盖刷新禁用、成功反馈、空态、错误态和重试；mock 延迟 `mockLatencyMs = 260` 保证 loading 可观察 |
| 详情/快捷入口有业务承接 | 已满足 | 查看详情弹窗；快捷入口跳转客户列表、会员等级、客户营销已有路由 |
| 固定 Chrome 目标站与本地取证 | 已满足 | `artifacts/screenshots|dom-snapshots|style-dumps|network/scrm--kehu-guanli--kehu-biaoqian/`，含 target、clone、empty-valid、error-valid 批次 |
| 实时记录追加 | 已满足 | `C:\Users\Administrator\Desktop\work\xxxxx\clone_pms_prd.md` 已追加客户标签执行记录和最终验证补充 |
| 不写入密码、cookie、token、storage state | 已满足 | 网络产物已清理手机号、token、appKey、accid、userId 等敏感预览字段；文件名不含凭证 |
| 自动化验证 | 已满足 | `npx eslint ... --no-cache` 退出码 0；`npx playwright test tests/customer-tag.spec.ts --timeout=60000 --workers=1 --reporter=line` 2/2 passed；`npm run build` 退出码 0，仅 chunk size warning |

## 剩余待确认

- `tagGroupName` 是否为正式筛选字段。
- 分页字段是否同时保留 `pageNum` 与 `current`。
- 新建标签组、导出、企微同步任务的正式接口 path、请求体和错误码。
- Hudson 原始响应包是否由后端统一改造，或继续由前端 adapter 转换。
