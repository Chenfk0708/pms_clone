# 客户营销完成审计

审计时间：2026-05-19

## 目标复述

读取并执行 `D:\pms_ui\95prompt\prompts111\042-scrm--yingxiao-tuiguang--kehu-yingxiao.md`：把现有客户营销页从静态顾问引导升级为可真实操作的 PMS 前端页面，完成统一 mock provider、交互闭环、接口文档、取证产物、自动化验证和实时记录。

## Prompt 到产物核对

| 要求 | 证据 | 状态 |
| --- | --- | --- |
| 读取新版 prompt 和原始 prompt | 已读取 `prompts111/042...md` 与 `prompts/042...md` | 完成 |
| 检查现有实现、路由、菜单、请求封装、测试 | 定位 `CustomerMarketingPage.*`、`App.tsx` 路由、`discovery.ts` 菜单、旧 `scrm-customer-marketing.spec.ts` | 完成 |
| 使用固定 Chrome 目标站取证 | `artifacts/screenshots/.../default-target-20260518T072939.png`、`artifacts/network/.../default-target-20260518T072939.json` | 完成 |
| 目标站网络与交互结论 | 目标站当前账号为 SCRM 顾问/订阅引导态，未暴露业务接口；已写入交互矩阵和实时记录 | 完成，真实业务字段待后端确认 |
| 交互矩阵 | `artifacts/interaction-matrix/scrm--yingxiao-tuiguang--kehu-yingxiao/interaction-matrix.md` | 完成 |
| 统一 mock provider | `src/services/customerMarketing.ts`，支持 `mock`/`real` provider 和 success/empty/error | 完成 |
| 统一响应包 | `code/message/data/traceId/timestamp` 在服务层、接口文档和本地数据服务清单中定义 | 完成 |
| 页面只消费适配后业务数据 | `CustomerMarketingPage.tsx` 通过 `loadCustomerMarketingData` 消费 `CustomerMarketingData` | 完成 |
| 可见交互有反馈 | 查询、重置、刷新、导出、批量触达、指标、漏斗、活动详情、跟进、客户详情、分页、快捷入口均有处理 | 完成 |
| loading/成功/失败/空态/禁用态 | 页面状态、notice、alert、empty、button disabled；Playwright 覆盖 empty/error | 完成 |
| 跨页入口使用已有路由 | `/customer/list`、`/customer/tag`、`/mallManagement/couponMgt`、`/order/house-order/list` | 完成 |
| 页面正文无开发态文案 | Playwright 使用 `forbiddenPageCopy` 断言；源码页面无禁用文案命中 | 完成 |
| 接口文档 | `D:\pms_ui\95prompt\接口文档\scrm--yingxiao-tuiguang--kehu-yingxiao-客户营销接口文档.md` | 完成 |
| 本地取证产物 | `default-clone-20260519T025620.png/html/json`、full 与导航状态截图 | 完成 |
| 自动化验证 | `tests/scrm-customer-marketing.spec.ts` 覆盖 mock 首屏、筛选、空态、错误态、按钮反馈、real provider | 完成 |
| 实时记录 | `C:\Users\Administrator\Desktop\work\xxxxx\clone_pms_prd.md` 已追加 2026-05-19 记录 | 完成 |
| 看板记录 | `D:\pms_ui\页面任务看板.md` 已追加续办记录 | 完成 |
| 不写入源码密钥/token | 本轮新增源码、测试、文档未写 cookie/token/storage state 内容 | 完成 |

## 验证命令

| 命令 | 结果 |
| --- | --- |
| `npx eslint src\services\customerMarketing.ts src\pages\CustomerMarketingPage.tsx tests\scrm-customer-marketing.spec.ts` | exit 0 |
| `PMS_TEST_BASE_URL=http://127.0.0.1:4198 npx playwright test tests/scrm-customer-marketing.spec.ts --timeout=60000 --workers=1 --reporter=line` | 4/4 passed |
| `npx tsc -p tsconfig.app.json --noEmit` | exit 0 |
| `npm run build` | exit 0，存在 Vite chunk size warning |
| `npm run lint` | exit 1，失败在既有 `DistributionListPage.tsx`、`WechatServicePage.tsx` React hooks 规则，非本页触达文件 |

## 未完全覆盖与剩余风险

1. 目标站当前账号没有暴露客户营销业务态，真实接口路径、字段枚举、分页和导出/批量触达接口仍需后端确认。
2. `npm run lint` 全量仍被其他页面既有问题阻断，本页触达文件 ESLint 已单独通过。
3. 本地业务页为了避开右侧全局会话浮层，在客户营销页 `page-content` 右侧预留了 344px 空间；窄屏下已恢复普通布局。

## 自评

评分：95/100。

扣分项：真实目标站未开放业务接口，接口契约仍属于前端草案，需要后端确认后才能进入联调态；全量 lint 有共享区既有失败。
