# 全员营销完成审计

任务 ID：`scrm--yingxiao-tuiguang--quanyuan-yingxiao`

审计日期：2026-05-19

## 范围核查

| 要求 | 结果 | 证据 |
| --- | --- | --- |
| 页面升级为可用前端页面 | 已完成 | `src/pages/FullMarketingPage.tsx` 消费服务层 view model，覆盖筛选、刷新、导出、邀请、编辑、二维码、空态、错误态 |
| 统一 mock/api provider | 已完成 | `src/services/fullMarketing.ts`，页面根节点暴露 `data-provider`、`data-trace-id`、`data-request-body` |
| 统一响应包与 adapter | 已完成 | `FullMarketingEnvelope`、`adaptCommission`、`adaptDistribution` |
| success/empty/error 三态 | 已完成 | `pms.fullMarketingMockMode=success/empty/error`，Playwright 专项覆盖 |
| 接口文档 | 已完成 | `D:\pms_ui\95prompt\接口文档\scrm--yingxiao-tuiguang--quanyuan-yingxiao-全员营销接口文档.md` |
| 交互矩阵 | 已完成 | `artifacts/interaction-matrix/scrm--yingxiao-tuiguang--quanyuan-yingxiao/interaction-matrix.md` |
| 固定 Chrome 取证 | 已完成 | 目标站批次 `20260518-95-target`、`20260518-95-target-contract`；本地批次 `20260519-95-local` |
| 页面正文禁用开发态文案 | 已验证 | `tests/full-marketing.spec.ts` 遍历禁止词 |
| 不写入账号/密码/cookie/token/storage state | 已复核 | 源码、文档和 artifacts 文件名未写入凭据内容 |

## 新增/修改文件

- `src/services/fullMarketing.ts`
- `src/pages/FullMarketingPage.tsx`
- `src/pages/FullMarketingPage.css`
- `tests/full-marketing.spec.ts`
- `scripts/capture-full-marketing.mjs`
- `artifacts/interaction-matrix/scrm--yingxiao-tuiguang--quanyuan-yingxiao/interaction-matrix.md`
- `artifacts/interaction-matrix/scrm--yingxiao-tuiguang--quanyuan-yingxiao/完成审计.md`
- `D:\pms_ui\95prompt\接口文档\scrm--yingxiao-tuiguang--quanyuan-yingxiao-全员营销接口文档.md`
- `C:\Users\Administrator\Desktop\work\xxxxx\clone_pms_prd.md`

## 本地取证

本地 clone 取证使用：

```bash
PMS_LOCAL_URL=http://127.0.0.1:4195/mallManagement/distribution PMS_CAPTURE_STAMP=20260519-95-local node scripts/capture-full-marketing.mjs --clone --state=<state>
```

已生成状态：

- `default`
- `data-tab`
- `invite`
- `edit`
- `empty`
- `error`

产物目录：

- `artifacts/screenshots/scrm--yingxiao-tuiguang--quanyuan-yingxiao/`
- `artifacts/dom-snapshots/scrm--yingxiao-tuiguang--quanyuan-yingxiao/`
- `artifacts/style-dumps/scrm--yingxiao-tuiguang--quanyuan-yingxiao/`
- `artifacts/network/scrm--yingxiao-tuiguang--quanyuan-yingxiao/`

说明：`empty` 取证的截图、DOM、style facts、network 已完整落盘；该脚本进程在收尾阶段超时，已终止残留进程。页面空态功能由专项 Playwright 继续作为验收依据。

## 验证命令

```bash
npx eslint src/services/fullMarketing.ts src/pages/FullMarketingPage.tsx tests/full-marketing.spec.ts scripts/capture-full-marketing.mjs
npx tsc -p tsconfig.app.json --noEmit
PMS_TEST_BASE_URL=http://127.0.0.1:4195 npx playwright test tests/full-marketing.spec.ts --timeout=60000 --workers=1 --reporter=line
```

结果：

- ESLint：退出码 0
- TypeScript：退出码 0
- Playwright 专项：5/5 passed

## 待后端确认

1. `promotionPlanProducts/page/get` 是否正式支持 `keyword`。
2. 分销计划保存接口的正式 method/path 与请求体。
3. 分销员汇总表接口路径。
4. 分销数据 `endDate` 是否按结束日后一天传入。
5. 二维码生成接口、有效期和绑定规则。
