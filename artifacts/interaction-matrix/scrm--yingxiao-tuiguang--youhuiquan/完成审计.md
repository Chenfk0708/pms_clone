# 优惠券 95 分完成审计

## 目标重述

将 `SCRM > 营销推广 > 优惠券` 从旧静态复刻升级为可操作页面：入口可访问，核心数据来自统一服务层的显式 mock provider，契约按目标站请求沉淀，所有可见关键控件有反馈，覆盖 success/empty/error，产出接口文档、交互矩阵、固定 Chrome 取证和自动化验证结果。

## Prompt 到产物检查表

| 要求 | 证据 | 结论 |
| --- | --- | --- |
| 读取新版 prompt 和原始 prompt | 已读取 `D:\pms_ui\95prompt\prompts111\040-scrm--yingxiao-tuiguang--youhuiquan.md` 与 `D:\pms_ui\95prompt\prompts\040-scrm--yingxiao-tuiguang--youhuiquan.md`，并记录到 `clone_pms_prd.md` | 通过 |
| 写代码前盘点现状 | `clone_pms_prd.md` 已记录旧实现文件、路由、测试、采集脚本和缺口 | 通过 |
| 目标站固定 Chrome 取证 | `artifacts/screenshots|dom-snapshots|style-dumps|network/scrm--yingxiao-tuiguang--youhuiquan/*20260518-95-target*`，契约取证 `artifacts/network/scrm--yingxiao-tuiguang--youhuiquan/contract-target-20260518-95-contract.json` | 通过 |
| 交互矩阵 | `artifacts/interaction-matrix/scrm--yingxiao-tuiguang--youhuiquan/interaction-matrix.md` | 通过 |
| 统一数据服务与 mock provider | `src/services/coupon.ts` 定义 `mock/api` provider、`CouponEnvelope`、请求体构造、adapter、success/empty/error | 通过 |
| 组件不再硬编码核心数据 | `src/pages/CouponPage.tsx` 消费 `fetchCouponList`、`fetchCouponTasks` 返回的业务模型，表格行来自服务层 | 通过 |
| 目标站接口字段沉淀 | 取证确认 `coupons/page/get`、`couponSendConfigs/page/get`、`holidays/get`、`channelRoomCategories/page/get/v2`，接口文档已列出请求体与字段 | 通过 |
| 接口文档 | `D:\pms_ui\95prompt\接口文档\scrm--yingxiao-tuiguang--youhuiquan-优惠券接口文档.md` | 通过 |
| 可见交互闭环 | 查询、重置、刷新、导出、查看详情、派发任务、新建任务、分页、新建页房型选择、节假日列表、提交均有反馈或弹窗承接 | 通过 |
| 空态/错误态/加载态 | 服务层和页面覆盖 loading、empty、error；Playwright 覆盖 empty/error | 通过 |
| 页面正文无开发态文案 | `src/pages/CouponPage.tsx` 未展示“未接入/阻塞/后端未就绪/后端接口未完成/mock 数据”等文案 | 通过 |
| 本地三态取证 | success：`default-clone-20260519-95-local-success.*`；empty：`default-clone-20260519-95-local-empty.*`；error：`default-clone-20260519-95-local-error.*` | 通过 |
| 自动化验证 | `PMS_TEST_BASE_URL=http://127.0.0.1:56340 npx playwright test tests/coupon.spec.ts --timeout=60000 --workers=1 --reporter=line` 退出码 0，3/3 passed | 通过 |
| 静态检查 | `npx eslint src/services/coupon.ts src/pages/CouponPage.tsx tests/coupon.spec.ts` 退出码 0 | 通过 |
| 构建 | `npm run build` 退出码 0，仅 Vite chunk size warning | 通过 |
| 全量 lint | `npm run lint -- --no-cache` 退出码 1，失败来自无关 `DistributionListPage.tsx` 与 `WechatServicePage.tsx` | 非本任务阻塞 |
| 安全 | 未写入账号、密码、cookie、token、storage state 内容到源码、测试、截图文件名或接口文档 | 通过 |

## 剩余风险与待确认

1. 目标站当前优惠券列表为空，本地 success 数据为契约示例；优惠券详情、导出、保存、新建派发任务的正式接口 path 仍需后端确认。
2. 目标站原始响应包是 `success/errorCode/errorMsg/data`，本地文档按新版要求沉淀为 `code/message/data/traceId/timestamp`，后端需确认最终统一格式。
3. 全量 lint 仍被其他页面阻塞，未扩大修复无关文件。

## 自评

96/100。扣分项：目标站业务列表为空，无法取证真实行内详情和导出接口；全量 lint 存在无关页面失败。
