# 批量加好友完成审计

任务：`scrm--zengzhang-huoke--piliang-jiahaoyou`

日期：`2026-05-19`

## 1. 目标重述

把 `/customer/addBatch` 从静态宣传页升级为可真实操作的 PMS 页面，并证明以下结果同时成立：

1. 项目入口、路由、菜单高亮、标题和布局状态正确。
2. 页面核心数据来自统一数据层的显式 provider，组件不直接硬编码业务数据。
3. provider 返回统一响应包，可沉淀为接口契约。
4. 可见按钮、筛选、下拉、详情、刷新、导出、快捷入口都有业务反馈。
5. empty/error/loading/disabled 关键状态可验证。
6. 目标站取证、本地取证、交互矩阵、接口文档、实时记录齐全。
7. 自动化验证覆盖关键数据展示、交互、错误态、空态和路由承接。
8. 页面正文不泄露开发态文案，不写入账号、密码、cookie、token 或 storage state 内容。

## 2. 提示词到证据清单

| 要求 | 当前证据 | 结论 |
| --- | --- | --- |
| 读取并执行原始 prompt | `../clone_pms_prd.md` 中 `2026-05-18 / scrm--zengzhang-huoke--piliang-jiahaoyou / 批量加好友 95 分真实交互与数据闭环启动` 记录已写明读取 `D:\pms_ui\95prompt\prompts\039-scrm--zengzhang-huoke--piliang-jiahaoyou.md` | 已完成 |
| 现有实现、路由、测试、脚本检查 | `../clone_pms_prd.md` 启动记录；路由见 `src/App.tsx` 中 `/customer/addBatch` 对应标题 `批量加好友` 与页面 `CustomerAddBatchPage` | 已完成 |
| 目标站固定 Chrome + storage state 取证 | `scripts/capture-customer-add-batch.mjs` 使用 `C:/Users/Administrator/AppData/Local/Google/Chrome/Bin/chrome.exe` 与 `playwright/.auth/pms-user.json`；目标站产物 `artifacts/screenshots/.../default-target-20260518153000-viewport.png`、`primary-target-20260518153100-viewport.png` 与对应 DOM/network 文件 | 已完成 |
| 交互矩阵必须先建模并替换为真实结果 | `artifacts/interaction-matrix/scrm--zengzhang-huoke--piliang-jiahaoyou/interaction-matrix.md` | 已完成 |
| 页面数据来自统一数据层显式 provider | `src/services/customerAddBatch.ts` 中 `fetchCustomerAddBatchDashboard`、`resolveCustomerAddBatchProvider`、`CustomerAddBatchProvider = 'mock' | 'api'`；页面 `src/pages/CustomerAddBatchPage.tsx` 仅消费 `CustomerAddBatchViewModel` | 已完成 |
| 统一响应包 | `src/services/customerAddBatch.ts` 中 `ApiEnvelope<T> = { code, message, data, traceId, timestamp }`；接口文档第 2 节 | 已完成 |
| mock provider 支持 success/empty/error | `src/services/customerAddBatch.ts` 中 `CustomerAddBatchMockState` 与 `fetchMockDashboard`；本地产物 `success-clone-20260519-business-provider-success-*`、`empty-clone-20260519-business-provider-empty-*`、`error-clone-20260519-business-provider-error-*` | 已完成 |
| 页面不在组件里硬编码业务数据 | 页面内仅保留静态宣传图片 `detailImages`；业务指标、候选客户、任务、趋势、筛选项全部来自 `viewModel` | 已完成 |
| 每个可见按钮和控件有处理结果 | `src/pages/CustomerAddBatchPage.tsx` 中查询、重置、刷新、导出、指标卡、快捷入口、候选客户行按钮、任务详情、订阅开通、弹窗关闭均有 handler；专项测试覆盖关键路径 | 已完成 |
| 跨页闭环 | 页面快捷入口跳转 `/customer/list`、`/customer/staffList`、`/customer/tag`；开通跳转 `/version/applicationPayment/detail`；专项测试已验证 | 已完成 |
| 错误闭环 | `src/pages/CustomerAddBatchPage.tsx` 中 `role="alert"` 错误区和 `重试`；`tests/customer-add-batch.spec.ts` 错误态用例 | 已完成 |
| empty/loading/disabled | 页面 `role="status"` 反馈区 + 按钮 `disabled={isLoading}`；`EmptyState` 组件；专项测试覆盖 empty 和交互刷新；body scan 验证三态正文 | 已完成 |
| 接口文档生成到指定路径 | `D:\pms_ui\95prompt\接口文档\scrm--zengzhang-huoke--piliang-jiahaoyou-批量加好友接口文档.md` | 已完成 |
| 实时记录追加取证结论和验收结果 | `C:\Users\Administrator\Desktop\work\xxxxx\clone_pms_prd.md` 已包含启动记录恢复摘录和本次最终验收记录 | 已完成 |
| 自动化验证命令和结果 | 新鲜命令见本文件第 3 节；当前结果为 ESLint 0 错误、Vite build 退出码 0、Playwright 5/5 通过 | 已完成 |
| 页面正文无开发态文案 | `tests/customer-add-batch.spec.ts` 对 success/empty/error 均断言；另有 2026-05-19 新鲜 body scan 输出 `forbidden=none` | 已完成 |
| 不写账号/密码/cookie/token/storage state 内容 | 代码和文档未包含账号密码；`scripts/capture-customer-add-batch.mjs` 仅引用 storage state 文件路径，不记录内容；`rg` 未命中账号密码/token/cookie 内容 | 已完成 |

## 3. 新鲜验证

### 3.1 定向 Lint

命令：

```powershell
npx eslint src/pages/CustomerAddBatchPage.tsx src/services/customerAddBatch.ts tests/customer-add-batch.spec.ts scripts/capture-customer-add-batch.mjs --no-cache
```

结果：退出码 `0`

### 3.2 临时构建

命令：

```powershell
npx vite build --outDir tmp/customer-add-batch-build --emptyOutDir true
```

结果：退出码 `0`；仅有 Vite chunk size warning，无构建失败。

### 3.3 专项 Playwright

命令：

```powershell
$env:PMS_TEST_BASE_URL='http://127.0.0.1:5642'
npx playwright test tests/customer-add-batch.spec.ts --config tmp/customer-add-batch.playwright.config.ts --timeout=60000 --workers=1 --reporter=line
```

结果：退出码 `0`，`5 passed (3.1m)`。

覆盖点：

- 首屏入口、菜单高亮、标题、provider 暴露
- 筛选参数传递
- 刷新、导出、指标详情、短信下发、标记已添加、客户详情、任务详情
- 重置按钮
- 快捷入口 `客户列表 / 企微员工列表 / 客户标签`
- empty/error
- 订阅详情跳转

### 3.4 正文开发态扫描

命令：使用固定 Chrome 访问 success/empty/error 三态，扫描正文是否包含 `mock 数据 / mock provider / provider=mock / 未接入 / 待接入 / 阻塞 / 后端未就绪 / 后端接口未完成`。

结果：

- `success: forbidden=none`
- `empty: forbidden=none`
- `error: forbidden=none`

## 4. 剩余未闭合项

以下项不阻塞前端任务完成，但需要后端联调确认：

1. 看板正式查询接口是否沿用 `/customer/addBatch/dashboard/get`。
2. `friendStatus`、`smsStatus`、`sourceChannel` 枚举是否复用全局口径。
3. 导出、短信下发、标记已添加的幂等键、异步任务和错误码规范。
4. 订阅开通态是否继续由 `edition/resource/get` 驱动。

## 5. 结论

基于当前源码、接口文档、交互矩阵、目标站取证、本地 success/empty/error 取证和新鲜验证结果，本任务的前端闭环要求已经满足。剩余事项属于后端正式接口确认，不阻塞当前 prompt 要求的页面交付与验收。
