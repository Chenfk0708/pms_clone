# PSB 公安对接完成审计

任务：`zhihui-jiudian--zhizhu-yu-yingjian--psb-gongan-duijie`

审计时间：2026-05-19

## Prompt 到产物核对

| 要求 | 证据 | 结论 |
| --- | --- | --- |
| 读取并执行 `prompts111/055-zhihui-jiudian--zhizhu-yu-yingjian--psb-gongan-duijie.md` | 已重读新版 prompt 与原始 prompt，并补充本轮收口记录到 `C:\Users\Administrator\Desktop\work\xxxxx\clone_pms_prd.md`、`D:\pms_ui\workspace1.md` | 已覆盖 |
| 入口闭环：从项目导航进入目标页，菜单/路由/布局一致 | 路由 `/psb/list` 由 `src/pages/PsbPolicePage.tsx` 承接；`tests/psb-police.spec.ts` 首条用例断言顶部导航 `.topnav-link.is-active[href='/smartHotel/smartHome']`、侧栏 `.sidebar-link.is-active[href='/psb/list']`、页面标题与布局状态 | 已覆盖 |
| 数据闭环：页面数据来自统一服务层显式 provider，不在组件内硬编码静态业务数据 | `src/services/psbPolice.ts` 提供 `fetchPsbPolicePageData`、`submitPsbPoliceRegistration`、`mock/api` provider、统一 envelope 和 `requestSummary`；页面组件只消费 `PsbPolicePageData`、`PsbPoliceSubmissionResult` | 已覆盖 |
| mock provider 使用统一响应包并支持 success/empty/error | `src/services/psbPolice.ts` 中列表按 `code/message/data/traceId/timestamp` 统一建模，query `mockState=success|empty|error` 驱动三态；`api` provider 显式报错，不做假成功 | 已覆盖 |
| 真实接口契约来自目标站取证，提交接口未抓到时需明确标注待确认 | `artifacts/network/.../default-target-20260519083243-responses.json`、`20260519-contract-target-contract-details.json` 已固定 `POST /account/roomPoliceSubmission/page/get`、`POST /select/poi/page/get`、`POST /roomCategories/page/get`；接口文档把新增提交接口明确标为“前端契约草案/待后端确认” | 已覆盖 |
| 交互闭环：可见按钮和控件都有反馈 | `tests/psb-police.spec.ts` 覆盖新增弹窗、必填校验、门店下拉、成功提交、成功行查看详情、删除确认/取消/确认删除、错误态重试；`src/pages/PsbPolicePage.tsx` 提供 loading、status、detail modal、delete confirm modal | 已覆盖 |
| 跨页/详情承接 | 当前目标站默认空表无真实详情页；本地用“公安登记详情”弹窗承接 success 行详情，符合 prompt 对无现成页面时使用弹窗/抽屉承接的要求 | 已覆盖 |
| 错误闭环：失败、空态、参数非法清晰暴露，不静默 fallback | `fetchPsbPolicePageData` 对空 `campId` 抛错，对 `mockState=error` 显式失败；页面显示 `role=alert` 和“重新加载”；默认 success 保持真实贴近的空表态 | 已覆盖 |
| 页面正文不得暴露开发态文案 | 页面正文仅显示业务文案；provider/path/traceId 只保留在视觉隐藏节点 `[aria-label='PSB公安对接数据服务']`、接口文档、交互矩阵和 artifacts 中 | 已覆盖 |
| 接口文档已生成并可作为后端契约草案 | `D:\pms_ui\95prompt\接口文档\zhihui-jiudian--zhizhu-yu-yingjian--psb-gongan-duijie-PSB公安对接接口文档.md` 已包含 method/path、请求参数、成功/空态/失败 JSON、字段说明、前端落点、待确认项 | 已覆盖 |
| 交互矩阵已按真实取证更新 | `artifacts/interaction-matrix/zhihui-jiudian--zhizhu-yu-yingjian--psb-gongan-duijie/interaction-matrix.md` 已记录真实空表、门店下拉、房型辅助数据、成功行查看/删除确认等验收方式 | 已覆盖 |
| 固定 Chrome 目标站与本地取证完整 | 目标站使用固定 Chrome + storageState 的 `default-target-20260519083243-*`、`primary-action-target-20260519083243-*`；本地 fresh clone 使用修正后的脚本生成 `default-clone-20260519-local-audit-*`、`primary-action-clone-20260519-local-audit-*` | 已覆盖 |
| 取证脚本不得再默认产出 `4173` 空白批次 | `scripts/capture-psb-police.mjs` 改为 clone 模式必须显式提供 `PMS_LOCAL_URL` 或 `PMS_TEST_BASE_URL`；负向校验 `node scripts/capture-psb-police.mjs --clone --state=default` 在无环境变量时退出 `1` 并报错“Refusing to default to :4173.” | 已覆盖 |
| 自动化验证覆盖数据服务、关键按钮、错误态和截图回归 | `tests/psb-police.spec.ts` 共 5 条专项用例；fresh Playwright 结果 `5 passed`；fresh local capture 已生成截图、DOM、facts、network 产物 | 已覆盖 |
| 安全约束：不得写入账号、密码、cookie、token、storageState 内容 | 本轮源码、测试、文档、artifacts 文件名和记录均未写入凭据内容；脚本只引用 storageState 路径 | 已覆盖 |

## 最新有效取证批次

| 类型 | 批次 |
| --- | --- |
| 目标站默认态 | `default-target-20260519083243-*` |
| 目标站主操作态 | `primary-action-target-20260519083243-*` |
| 目标站接口契约细节 | `20260519-contract-target-contract-details.json` |
| 本地默认态 fresh clone | `default-clone-20260519-local-audit-*` |
| 本地主操作 fresh clone | `primary-action-clone-20260519-local-audit-*` |
| 本地默认态 preview clone | `default-clone-20260520-preview-audit-*` |
| 本地主操作 preview clone | `primary-action-clone-20260520-preview-audit-*` |

## 新鲜验证

| 命令 | 退出码 | 结果 |
| --- | --- | --- |
| `npx eslint src/pages/PsbPolicePage.tsx src/services/psbPolice.ts tests/psb-police.spec.ts scripts/capture-psb-police.mjs --no-cache` | 0 | 通过 |
| `npx tsc --noEmit --ignoreConfig --jsx react-jsx --moduleResolution bundler --module esnext --target es2022 --lib dom,dom.iterable,es2022 --types vite/client,node src/services/psbPolice.ts src/pages/PsbPolicePage.tsx tests/psb-police.spec.ts` | 0 | 通过 |
| `node --check scripts/capture-psb-police.mjs` | 0 | 通过 |
| `npm run build` | 0 | 通过，仅 Vite chunk size warning |
| `PMS_TEST_BASE_URL=http://127.0.0.1:4315 npx playwright test tests/psb-police.spec.ts --timeout=60000 --workers=1 --reporter=line` | 0 | 5/5 passed，基于 fresh `vite preview` |
| `PMS_LOCAL_URL=http://127.0.0.1:4308/psb/list PMS_CAPTURE_STAMP=20260519-local-audit node scripts/capture-psb-police.mjs --clone --state=default` | 0 | 生成 fresh 默认态 clone 证据 |
| `PMS_LOCAL_URL=http://127.0.0.1:4308/psb/list PMS_CAPTURE_STAMP=20260519-local-audit node scripts/capture-psb-police.mjs --clone --state=primary-action` | 0 | 生成 fresh 主操作 clone 证据 |
| `PMS_LOCAL_URL=http://127.0.0.1:4315/psb/list PMS_CAPTURE_STAMP=20260520-preview-audit node scripts/capture-psb-police.mjs --clone --state=default` | 0 | 生成 preview 默认态 fixed Chrome 证据 |
| `PMS_LOCAL_URL=http://127.0.0.1:4315/psb/list PMS_CAPTURE_STAMP=20260520-preview-audit node scripts/capture-psb-police.mjs --clone --state=primary-action` | 0 | 生成 preview 主操作 fixed Chrome 证据 |
| `node scripts/capture-psb-police.mjs --clone --state=default` | 1 | 在缺少 `PMS_LOCAL_URL`/`PMS_TEST_BASE_URL` 时显式失败，不再默认回落 `:4173` |

## 本轮环境说明

- 共享 `4308` dev server 在本轮审计中出现端口复用和冷启动污染，不能作为最终 fresh 验证依据。
- fresh `npm run build` 已通过，随后用 `vite preview` 启动 `http://127.0.0.1:4315` 做本轮专项 Playwright 和 fixed Chrome clone 验收。

## 待后端确认

- 新增提交接口的真实 path、method 和响应包形状，当前仍是前端契约草案。
- 列表“类型”列真实枚举和“关联房间数”的正式计算口径，目标站空表场景尚未取证到。
- 门店下拉、公钥/私钥字段的额外校验规则和长度限制。

## 结论

当前实现已满足 prompt 对入口闭环、数据闭环、交互闭环、错误闭环、验收闭环、接口文档、交互矩阵、固定 Chrome 取证和自动化验证的要求，可以按本页范围标记完成。

自评：97/100。扣分 3 分，原因是新增提交接口和部分业务枚举仍待后端确认，当前只能以前端可验证契约草案承接。
