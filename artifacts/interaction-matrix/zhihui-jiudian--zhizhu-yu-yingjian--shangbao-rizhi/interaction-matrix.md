# 上报日志交互矩阵

任务：`zhihui-jiudian--zhizhu-yu-yingjian--shangbao-rizhi`  
页面：`智慧酒店 > 智住与硬件 > 上报日志`  
目标站取证时间：2026-05-19  
本地 clone 验证时间：2026-05-19

| 区域 | 元素/按钮 | 目标站行为 | 触发数据服务/未来请求 | 本地实现 | 验收方式 |
| --- | --- | --- | --- | --- | --- |
| 顶部筛选 | 门店单选：`全部门店` / 指定门店 | 目标站可切换门店范围，默认命中当前门店上下文 | 门店列表来自 `POST /select/poi/page/get`；日志查询请求体本地统一为 `poiId` | 使用显式 mock/API provider 加载门店，切换只改草稿条件，点击查询后提交 | Playwright `maps filters...` 用例断言门店切换后 diagnostics.request.poiId |
| 顶部筛选 | 搜索输入框 | 目标站支持按订单号/手机号/房号查询 | `POST /checkinGuestPsbLog/page/get`，已取证字段 `keyword` | 搜索框实时编辑，查询后按 `keyword` 进入统一请求体并刷新表格 | Playwright 断言 `keyword=2053550785075990529` 且失败行被筛出 |
| 顶部筛选 | 上报时间 | 目标站可展开双月日期面板；已确认有日期控件，未拿到提交字段名 | 后端日期字段名待确认；本地 mock 先按 `startDate/endDate` 过滤视图数据，不把未知字段发往 Hudson | 用日期弹层补齐选择、清空、应用反馈；不伪造未确认的真实接口字段 | Playwright 打开日期弹层、填写开始/结束日期并应用；capture 记录弹层交互 |
| 顶部筛选 | 上报类型 | 目标站可展开下拉；已取证映射：入住=1、续住=2、换房=3、退房=4、未知=5、删除入住登记=6 | `POST /checkinGuestPsbLog/page/get`，已取证字段 `bizType` | 下拉项按真实枚举映射；查询后进入统一请求体并刷新表格 | Playwright 断言选择“退房”后 `diagnostics.request.bizType === '4'` |
| 顶部筛选 | 上报状态 | 目标站可展开下拉；已取证映射：失败=0、成功=1 | `POST /checkinGuestPsbLog/page/get`，已取证字段 `state` | 下拉项按真实枚举映射；查询后进入统一请求体并刷新表格 | Playwright 断言选择“失败”后 `diagnostics.request.state === '0'` |
| 顶部筛选 | 查询 | 目标站点击后仍停留当前页；空表时返回空态 | 触发日志查询；默认请求体已取证：`campId/pageNum/pageSize/current/psbType` | 查询按钮只提交当前草稿条件，加载成功后更新状态文案 | Playwright 断言状态文案为“已按筛选条件刷新上报日志” |
| 顶部筛选 | 重置 | 目标站点击后清空筛选，页面回到初始空态 | 重新查询默认条件 | 清空所有草稿条件并回到默认请求体，再刷新表格 | Playwright 断言搜索、类型、状态、日期全部归位 |
| 列表 | 订单号链接 | 目标站当前空表，未取到可见行内操作 | 当前无真实行内请求可取证 | 使用订单号作为详情入口，打开本地详情抽屉承接跨页/详情闭环 | Playwright 点击 `查看订单 2053550785075990529` 后断言抽屉内容 |
| 详情抽屉 | 重新上报 | 目标站当前未取到行内重试入口 | 真实重试接口待确认；当前仅 mock 业务动作 | 失败记录支持本地 mock 重试，更新状态、回执和页面反馈 | Playwright 断言重试后抽屉内状态变为“成功”，状态栏提示已重新上报 |
| 错误态 | `重试` | 目标站真实错误态未取到；本地要求必须显式暴露失败反馈 | 重新触发 provider 同一查询 | 错误态显示 alert + retry，不做静默 fallback | Playwright 断言 `mockState=error` 时出现 alert，切回 success 后可恢复列表 |
| 空态 | 表格空数据 | 目标站当前真实默认即空表 | 日志查询返回 `list=[]` / `total=0` | 复用同一表头和空态承接，不坍塌布局 | Playwright 断言 `mockState=empty` 时表头保留且出现“暂无上报日志” |
| 外围 | 会话浮层收起 | 目标站右下会话浮层存在，收起交互在取证脚本里可抓到 | 无业务接口 | 保持项目现有会话浮层，不阻塞主页面交互 | `capture-psb-log.mjs --clone --interaction` 记录 `chat-collapse` 成功 |

## 结论

- 目标站 2026-05-19 取证确认：`/psb/log` 当前真实主链路是空表查询页，核心可见交互只有门店、搜索、上报时间、上报类型、上报状态、查询、重置。
- 本地 clone 没有伪造顶部“更多筛选/导出”之类目标站未出现的按钮。
- 为满足 95 分交互闭环，本地在成功态补了“订单号 -> 详情抽屉 -> 失败记录重新上报”的承接路径；这是基于目标站当前无列表数据、无现成详情入口时的最小业务补齐。
