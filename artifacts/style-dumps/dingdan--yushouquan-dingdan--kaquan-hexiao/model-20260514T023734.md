# 卡券核销页面建模记录

- 任务 ID：`dingdan--yushouquan-dingdan--kaquan-hexiao`
- 目标 URL：`https://minsubao.localhome.cn/mallManagement/verificationManagement`
- 本地 URL：`http://127.0.0.1:4209/mallManagement/verificationManagement`
- 取证批次：target `20260514T022112`，clone `20260514T023734`

## 页面布局树

1. 顶部全局导航：真实站 `订单` 高亮；本地复用既有 `AppShell` 订单高亮逻辑。
2. 左侧订单菜单：`住宿订单`、`长租订单`、`预售券订单`、`卡券核销`、`酒店套餐订单`；当前页 `卡券核销` 高亮。
3. 业务主区：
   - 卡券码输入行：`请输入卡券码` 输入框，右侧 `核 销` 主按钮。
   - 记录区标题行：左侧 `核销记录`，右侧 `导出明细` 主按钮。
   - 核销记录表格：11 列表头，当前为空态。

## 组件清单

- `CardVerificationPage`：本页局部页面组件。
- `card-verify-entry`：卡券码输入与提交。
- `card-verify-records`：核销记录容器。
- `card-verify-table`：固定表头和空态。
- `card-verify-notice`：仅用于本地交互反馈，不声明真实核销成功。

## 状态矩阵

| 状态 | 目标事实 | 本地实现 |
| --- | --- | --- |
| 默认态 | 输入框、`核 销`、`核销记录`、`导出明细`、空表格 | 已对齐 |
| 输入卡券码 | 目标可输入 `请输入卡券码` | 已支持输入 |
| 点击核销 | 目标触发后台核销链路，当前未构造真实卡券码 | 本地展示已输入卡券码反馈，不伪造核销成功 |
| 导出明细 | 目标有按钮入口 | 本地保留按钮并给出触发反馈 |

## 视觉 Token 清单

- 主色：`rgb(77, 101, 246)`。
- 输入框：`500px x 28px`，`border: 1px solid rgb(217, 217, 217)`，`border-radius: 5px`，`padding: 4px 11px`。
- 主按钮：高度 `28px`，白字，蓝底，`border-radius: 4px`。
- 表格：`x=172 y=140 width=1252`，表头高 `56px`，背景 `#fafafa`，边框 `#f0f0f0`。
- 空态：`暂无数据`，颜色 `rgba(0, 0, 0, 0.25)`。

## 交互清单

- 输入卡券码：支持键入和 Enter 提交。
- 核销按钮：空值提示 `请输入卡券码`；非空显示输入值，避免伪造真实核销成功。
- 导出明细：保留入口并给出触发反馈。

## 剩余差异

- 全局 AppShell 与真实 PMS AntD 顶栏/侧栏、字体图标、运行时会话浮层仍存在差异。
- 目标页当前为空态，无真实卡券数据行可采集；后续如账号出现数据，需要补充行态和操作态。
