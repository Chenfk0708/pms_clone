# 企微员工列表目标页面模型

- TASK_ID：`scrm--qiwei-yuangong-guanli--qiwei-yuangong-liebiao`
- TARGET_URL：`https://minsubao.localhome.cn/customer/staffList`
- 采集方式：无头 Playwright，固定 Chrome，复用 `playwright/.auth/pms-user.json`
- 当前账号业务态：未开通企微 SCRM，显示订阅引导，不显示员工数据表

## 页面骨架

1. 顶部主导航：`SCRM` 高亮。
2. 左侧 SCRM 二级导航：分组包含客户概况、客户管理、会员中心、增长获客、营销推广、客户沟通、企微员工管理。
3. 当前高亮项：`企微员工管理 > 企微员工列表`。
4. 主内容为白底圆角卡片；本地通用页面大标题在目标站不可见。

## 默认态

- 顶部产品信息区：
  - 图标：企微 SCRM 红色楼体图标。
  - 标题：`企微SCRM-员工管理`。
  - 说明：`实时获取企业微信员工，实现员工管理`。
  - 主按钮：`立即开通`。
  - 橙色胶囊角标：`限时免费`。
- 商品详情区：
  - 标题：`商品详情`。
  - 展示多张 SCRM 宣传图；首屏主要图文为 `企微SCRM 高效获客留存`。
- 右下角会话侧栏：
  - 标题 `全部会话`。
  - 可折叠按钮 `收 起`。
  - 属于全局运行时浮层，视觉 diff 可单独解释。

## 关键交互

- 点击 `立即开通`：跳转至 `/version/applicationPayment/detail` 的企微 SCRM 购买详情。
- 点击 `商品详情`：同样进入企微 SCRM 购买详情。
- 购买详情可见：
  - 标题：`企微SCRM`。
  - 说明：企业微信入住全流程体验升级。
  - 右侧 `购买信息`。
  - 商品价格 `¥150.6`，原价 `¥75,300/年`。
  - 购买时长：`跟随版本2027-09-28到期`。
  - 协议勾选和 `立即购买`。

## 本地复刻策略

- 只复刻当前账号真实可见的未开通引导态，不伪造员工表格。
- 本地 `立即开通` 在页面内切换到购买详情态，避免误跳既有智能保洁订阅页。
- 使用目标公开图片资源落地到 `public/scrm-assets/`，保证本地截图稳定。
- 共享接入需要最小修改：
  - `src/App.tsx`：新增 `/customer/staffList` 路由、页面标题、导入 `StaffListPage`。
  - `src/components/AppShell.tsx`：将 `/customer/*` 识别为 SCRM 顶部与侧栏上下文。
  - `src/data/discovery.ts`：将 `企微员工列表` 路径从 `/scrm/businessManage/employeesList` 调整为真实 `/customer/staffList`。

## 剩余差异预期

- 全局顶栏和真实 PMS AntD 字体图标会有细节差异。
- 右下角会话浮层为动态生产数据，本地使用既有 `ChatDock` 近似。
- 购买详情本地使用页面内状态切换，不复制真实订阅中心完整路由树。
