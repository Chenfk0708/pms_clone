# 接待配置 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将 `/scrm/wechatService/receptionConfig` 从静态未开通营销页升级为显式 provider 驱动的接待配置业务页，具备查询、重置、刷新、保存、预览、导出、快捷入口和 success/empty/error 闭环。

**Architecture:** 新增 `src/services/receptionConfig.ts` 作为统一数据服务层，负责 provider 切换、统一响应包、mock 契约、字段适配和诊断写入；`ReceptionConfigPage` 只消费业务模型并承接页面交互。测试先行，先用 Playwright 重写接待配置专项，验证本页 provider 契约、可见交互和跨页承接，再用最小实现让测试转绿。

**Tech Stack:** React、TypeScript、React Router、Playwright、Vite

---

### Task 1: 重写接待配置专项测试

**Files:**
- Modify: `tests/reception-config.spec.ts`

- [ ] **Step 1: Write the failing test**

```ts
test('/scrm/wechatService/receptionConfig loads provider-driven dashboard', async ({ page }) => {
  await page.goto(appUrl('/scrm/wechatService/receptionConfig'))

  await expect(page.locator('.reception-config-page')).toHaveAttribute('data-provider', 'mock')
  await expect(page.getByRole('heading', { name: '接待配置中心' })).toBeVisible()
  await expect(page.getByLabel('接待配置核心指标')).toContainText('接待员工')
})
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npx playwright test tests/reception-config.spec.ts --timeout=60000 --workers=1 --reporter=line`
Expected: FAIL，因为当前页面仍是旧的静态未开通营销页，找不到业务页标题和 provider 属性。

- [ ] **Step 3: Expand tests for full closure**

```ts
test('/scrm/wechatService/receptionConfig handles filters, preview, save, export, and routes', async ({ page }) => {
  await page.goto(appUrl('/scrm/wechatService/receptionConfig'))
  await page.getByRole('button', { name: '查询' }).click()
  await page.getByRole('button', { name: '预览欢迎语' }).click()
  await page.getByRole('button', { name: '保存配置' }).click()
  await page.getByRole('button', { name: '导出配置' }).click()
  await page.getByRole('button', { name: '微信客服' }).click()
})
```

- [ ] **Step 4: Run test to verify it still fails for the right reason**

Run: `npx playwright test tests/reception-config.spec.ts --timeout=60000 --workers=1 --reporter=line`
Expected: FAIL，且失败点集中在接待配置业务结构、交互反馈和路由承接未实现，而不是测试语法错误。

### Task 2: 新增接待配置服务层

**Files:**
- Create: `src/services/receptionConfig.ts`
- Modify: `src/pages/ReceptionConfigPage.tsx`

- [ ] **Step 1: Write a minimal service contract to satisfy the first test**

```ts
export type ReceptionConfigProvider = 'mock' | 'api'
export type ReceptionConfigMockState = 'success' | 'empty' | 'error'

export function resolveReceptionConfigRuntimeConfig(location: Pick<Location, 'search'>) {
  const params = new URLSearchParams(location.search)
  return {
    provider: params.get('receptionConfigProvider') === 'api' ? 'api' : 'mock',
    mockState: params.get('receptionConfigMockState') === 'empty' ? 'empty' : 'success',
  } as const
}
```

- [ ] **Step 2: Run test to verify it still fails**

Run: `npx playwright test tests/reception-config.spec.ts --timeout=60000 --workers=1 --reporter=line`
Expected: FAIL，因为页面还未消费服务层。

- [ ] **Step 3: Implement the minimal fetch function and diagnostics**

```ts
export async function fetchReceptionConfigDashboard(query: ReceptionConfigQuery, signal?: AbortSignal) {
  const envelope = await fetchMockReceptionConfig(query, signal)
  const data = unwrapEnvelope(envelope)
  writeDiagnostics({ endpoint: RECEPTION_CONFIG_MOCK_ENDPOINT, provider: 'mock', state: query.mockState ?? 'success', traceId: envelope.traceId, request: buildRequest(query) })
  return { ...data, provider: 'mock', state: query.mockState ?? 'success', traceId: envelope.traceId, timestamp: envelope.timestamp }
}
```

- [ ] **Step 4: Run test to verify the service contract is usable**

Run: `npx playwright test tests/reception-config.spec.ts --timeout=60000 --workers=1 --reporter=line`
Expected: FAIL，但错误点前移到页面结构缺失，而不是服务层缺失。

### Task 3: 实现接待配置页面业务 UI

**Files:**
- Modify: `src/pages/ReceptionConfigPage.tsx`
- Modify: `src/pages/ReceptionConfigPage.css`

- [ ] **Step 1: Implement the smallest page shell that satisfies the first screen assertions**

```tsx
return (
  <div className="reception-config-page" data-provider={view?.provider ?? 'mock'} data-response-state={view?.state ?? 'success'}>
    <header className="reception-config-header">
      <h1>接待配置中心</h1>
      <p>统一配置企微接待员工、欢迎语、客户备注和自动分配规则。</p>
    </header>
  </div>
)
```

- [ ] **Step 2: Run test to verify the first test passes and the next one fails**

Run: `npx playwright test tests/reception-config.spec.ts --timeout=60000 --workers=1 --reporter=line`
Expected: 第一条测试 PASS，后续交互测试 FAIL。

- [ ] **Step 3: Add filters, metrics, config cards, dialogs, feedback, quick routes, empty and error states**

```tsx
<section aria-label="接待配置筛选">...</section>
<section aria-label="接待配置核心指标">...</section>
<section aria-label="接待规则列表">...</section>
<section aria-label="快捷入口">...</section>
{dialog ? <Dialog ... /> : null}
```

- [ ] **Step 4: Run test to verify all reception-config specs pass**

Run: `npx playwright test tests/reception-config.spec.ts --timeout=60000 --workers=1 --reporter=line`
Expected: PASS

### Task 4: 补文档与取证产物

**Files:**
- Create: `D:/pms_ui/95prompt/接口文档/scrm--kehu-goutong--jiedai-peizhi-接待配置接口文档.md`
- Create: `artifacts/interaction-matrix/scrm--kehu-goutong--jiedai-peizhi/interaction-matrix.md`
- Modify: `C:/Users/Administrator/Desktop/work/xxxxx/clone_pms_prd.md`

- [ ] **Step 1: Write the interface document**

```md
# 接待配置接口文档
## 1. 查询接待配置看板
## 2. 保存接待配置
## 3. 导出接待配置
```

- [ ] **Step 2: Write the interaction matrix**

```md
| 区域 | 元素/按钮 | 目标行为 | 数据服务 | 本地实现 | 验收方式 |
| --- | --- | --- | --- | --- | --- |
```

- [ ] **Step 3: Append execution evidence to the realtime record**

```md
## 2026-05-19 / `scrm--kehu-goutong--jiedai-peizhi`
- 已新增 receptionConfig provider、页面交互闭环、接口文档、交互矩阵和专项验证。
```

- [ ] **Step 4: Run file existence checks**

Run: `Get-ChildItem 'D:/pms_ui/95prompt/接口文档/scrm--kehu-goutong--jiedai-peizhi-接待配置接口文档.md','artifacts/interaction-matrix/scrm--kehu-goutong--jiedai-peizhi/interaction-matrix.md'`
Expected: Both files exist

### Task 5: 完成最终验证

**Files:**
- Test: `tests/reception-config.spec.ts`

- [ ] **Step 1: Run the focused Playwright suite**

Run: `npx playwright test tests/reception-config.spec.ts --timeout=60000 --workers=1 --reporter=line`
Expected: PASS

- [ ] **Step 2: Run touched-file lint**

Run: `npx eslint src/pages/ReceptionConfigPage.tsx src/pages/ReceptionConfigPage.css src/services/receptionConfig.ts tests/reception-config.spec.ts --no-cache`
Expected: PASS

- [ ] **Step 3: Run build**

Run: `npm run build`
Expected: PASS，或明确记录与本页无关的现存阻塞项

- [ ] **Step 4: Capture final evidence**

Run: `git diff -- src/pages/ReceptionConfigPage.tsx src/pages/ReceptionConfigPage.css src/services/receptionConfig.ts tests/reception-config.spec.ts`
Expected: Shows only reception-config scoped changes
