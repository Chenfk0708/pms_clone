# 中央价格日历滚动布局修正 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让中央价格页改为仅右侧日历矩阵横向滚动，同时保持顶部日期头和左侧房型列固定，对齐目标站的基础布局与交互。

**Architecture:** 保留现有 `PricePage` 的业务数据流和单元格渲染逻辑，只重构中央价格视图的 DOM 分层。把当前单一 `.price-grid` 滚动容器拆成“固定头层 + 左列层 + 右侧横向滚动层”，通过共享列宽常量和滚动同步保持表头、主体一致。

**Tech Stack:** React 19、TypeScript、Vite、Playwright

---

### Task 1: 锁定滚动行为回归

**Files:**
- Modify: `tests/central-price.spec.ts`
- Test: `tests/central-price.spec.ts`

- [ ] **Step 1: 写失败测试**

```ts
test('/houseManage/houseCale keeps header and first column fixed while the calendar area scrolls horizontally', async ({ page }) => {
  await page.setViewportSize({ width: 1440, height: 900 })
  await page.addInitScript(() => {
    window.localStorage.setItem('pms.centralPriceProvider', 'real')
  })
  await page.route(centralPriceEndpoint, async (route) => {
    await route.fulfill({ json: centralPriceResponse })
  })

  await page.goto('/houseManage/houseCale')

  const matrixScroll = page.getByTestId('central-price-matrix-scroll')
  const matrixHeader = page.getByTestId('central-price-matrix-header')
  const firstColumn = page.getByTestId('central-price-matrix-row-header').first()

  const beforeHeaderX = await matrixHeader.evaluate((node) => node.getBoundingClientRect().x)
  const beforeColumnX = await firstColumn.evaluate((node) => node.getBoundingClientRect().x)

  await matrixScroll.evaluate((node) => {
    node.scrollLeft = 480
    node.dispatchEvent(new Event('scroll'))
  })

  await expect(matrixScroll).toHaveJSProperty('scrollLeft', 480)
  await expect.poll(async () => matrixHeader.evaluate((node) => node.getBoundingClientRect().x)).toBe(beforeHeaderX)
  await expect.poll(async () => firstColumn.evaluate((node) => node.getBoundingClientRect().x)).toBe(beforeColumnX)
})
```

- [ ] **Step 2: 运行测试并确认失败**

Run: `npx playwright test tests/central-price.spec.ts --grep "keeps header and first column fixed" --reporter=line --workers=1`
Expected: FAIL，因为测试节点不存在或滚动后头部/左列跟随位移。

- [ ] **Step 3: 实现最小结构改造**

```tsx
<section className="central-price-matrix">
  <div className="central-price-matrix__header" data-testid="central-price-matrix-header">
    ...
  </div>
  <div className="central-price-matrix__body">
    <div className="central-price-matrix__left">...</div>
    <div className="central-price-matrix__scroll" data-testid="central-price-matrix-scroll">
      ...
    </div>
  </div>
</section>
```

- [ ] **Step 4: 运行测试并确认通过**

Run: `npx playwright test tests/central-price.spec.ts --grep "keeps header and first column fixed" --reporter=line --workers=1`
Expected: PASS

### Task 2: 对齐中央价格页结构与样式

**Files:**
- Modify: `src/pages/PricePage.tsx`
- Modify: `src/pages/PricePage.css`
- Test: `tests/central-price.spec.ts`

- [ ] **Step 1: 保留现有业务逻辑，拆出中央价格专用矩阵结构**

```tsx
const centralColumnTemplate = `280px 112px 112px repeat(${visibleDates.length}, 96px)`
```

```tsx
{isCentral ? (
  <div className="central-price-matrix">
    ...
  </div>
) : (
  <div className="price-grid">...</div>
)}
```

- [ ] **Step 2: 用 CSS 建立固定头、固定左列、右侧滚动层**

```css
.central-price-matrix {
  display: grid;
  grid-template-rows: auto minmax(0, 1fr);
  height: 100%;
  overflow: hidden;
}

.central-price-matrix__body {
  display: grid;
  grid-template-columns: 280px minmax(0, 1fr);
  min-height: 0;
}

.central-price-matrix__scroll {
  overflow-x: auto;
  overflow-y: hidden;
}
```

- [ ] **Step 3: 让中央价格行高、底色、周末色块更接近目标站**

```css
.central-price-matrix__header,
.central-price-matrix__row {
  border-bottom: 1px solid #e8edf5;
}
```

- [ ] **Step 4: 运行中央价格全量测试**

Run: `npx playwright test tests/central-price.spec.ts --reporter=line --workers=1`
Expected: PASS

- [ ] **Step 5: 运行类型检查**

Run: `npx tsc -b`
Expected: PASS
